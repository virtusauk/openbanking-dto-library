// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TFRiskType is an object representing the database table.
type TFRiskType struct {
	RiskTypeID   int         `boil:"risk_type_id" json:"risk_type_id" toml:"risk_type_id" yaml:"risk_type_id"`
	RiskType     string      `boil:"risk_type" json:"risk_type" toml:"risk_type" yaml:"risk_type"`
	Description  string      `boil:"description" json:"description" toml:"description" yaml:"description"`
	MakerDate    time.Time   `boil:"maker_date" json:"maker_date" toml:"maker_date" yaml:"maker_date"`
	CheckerDate  null.Time   `boil:"checker_date" json:"checker_date,omitempty" toml:"checker_date" yaml:"checker_date,omitempty"`
	MakerID      string      `boil:"maker_id" json:"maker_id" toml:"maker_id" yaml:"maker_id"`
	CheckerID    null.String `boil:"checker_id" json:"checker_id,omitempty" toml:"checker_id" yaml:"checker_id,omitempty"`
	ModifiedBy   null.String `boil:"modified_by" json:"modified_by,omitempty" toml:"modified_by" yaml:"modified_by,omitempty"`
	ModifiedDate null.Time   `boil:"modified_date" json:"modified_date,omitempty" toml:"modified_date" yaml:"modified_date,omitempty"`

	R *tFRiskTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tFRiskTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TFRiskTypeColumns = struct {
	RiskTypeID   string
	RiskType     string
	Description  string
	MakerDate    string
	CheckerDate  string
	MakerID      string
	CheckerID    string
	ModifiedBy   string
	ModifiedDate string
}{
	RiskTypeID:   "risk_type_id",
	RiskType:     "risk_type",
	Description:  "description",
	MakerDate:    "maker_date",
	CheckerDate:  "checker_date",
	MakerID:      "maker_id",
	CheckerID:    "checker_id",
	ModifiedBy:   "modified_by",
	ModifiedDate: "modified_date",
}

// Generated where

var TFRiskTypeWhere = struct {
	RiskTypeID   whereHelperint
	RiskType     whereHelperstring
	Description  whereHelperstring
	MakerDate    whereHelpertime_Time
	CheckerDate  whereHelpernull_Time
	MakerID      whereHelperstring
	CheckerID    whereHelpernull_String
	ModifiedBy   whereHelpernull_String
	ModifiedDate whereHelpernull_Time
}{
	RiskTypeID:   whereHelperint{field: `risk_type_id`},
	RiskType:     whereHelperstring{field: `risk_type`},
	Description:  whereHelperstring{field: `description`},
	MakerDate:    whereHelpertime_Time{field: `maker_date`},
	CheckerDate:  whereHelpernull_Time{field: `checker_date`},
	MakerID:      whereHelperstring{field: `maker_id`},
	CheckerID:    whereHelpernull_String{field: `checker_id`},
	ModifiedBy:   whereHelpernull_String{field: `modified_by`},
	ModifiedDate: whereHelpernull_Time{field: `modified_date`},
}

// TFRiskTypeRels is where relationship names are stored.
var TFRiskTypeRels = struct {
	RiskTypeTFRisks string
}{
	RiskTypeTFRisks: "RiskTypeTFRisks",
}

// tFRiskTypeR is where relationships are stored.
type tFRiskTypeR struct {
	RiskTypeTFRisks TFRiskSlice
}

// NewStruct creates a new relationship struct
func (*tFRiskTypeR) NewStruct() *tFRiskTypeR {
	return &tFRiskTypeR{}
}

// tFRiskTypeL is where Load methods for each relationship are stored.
type tFRiskTypeL struct{}

var (
	tFRiskTypeColumns               = []string{"risk_type_id", "risk_type", "description", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	tFRiskTypeColumnsWithoutDefault = []string{"risk_type_id", "risk_type", "description", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	tFRiskTypeColumnsWithDefault    = []string{}
	tFRiskTypePrimaryKeyColumns     = []string{"risk_type_id"}
)

type (
	// TFRiskTypeSlice is an alias for a slice of pointers to TFRiskType.
	// This should generally be used opposed to []TFRiskType.
	TFRiskTypeSlice []*TFRiskType
	// TFRiskTypeHook is the signature for custom TFRiskType hook methods
	TFRiskTypeHook func(context.Context, boil.ContextExecutor, *TFRiskType) error

	tFRiskTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tFRiskTypeType                 = reflect.TypeOf(&TFRiskType{})
	tFRiskTypeMapping              = queries.MakeStructMapping(tFRiskTypeType)
	tFRiskTypePrimaryKeyMapping, _ = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, tFRiskTypePrimaryKeyColumns)
	tFRiskTypeInsertCacheMut       sync.RWMutex
	tFRiskTypeInsertCache          = make(map[string]insertCache)
	tFRiskTypeUpdateCacheMut       sync.RWMutex
	tFRiskTypeUpdateCache          = make(map[string]updateCache)
	tFRiskTypeUpsertCacheMut       sync.RWMutex
	tFRiskTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tFRiskTypeBeforeInsertHooks []TFRiskTypeHook
var tFRiskTypeBeforeUpdateHooks []TFRiskTypeHook
var tFRiskTypeBeforeDeleteHooks []TFRiskTypeHook
var tFRiskTypeBeforeUpsertHooks []TFRiskTypeHook

var tFRiskTypeAfterInsertHooks []TFRiskTypeHook
var tFRiskTypeAfterSelectHooks []TFRiskTypeHook
var tFRiskTypeAfterUpdateHooks []TFRiskTypeHook
var tFRiskTypeAfterDeleteHooks []TFRiskTypeHook
var tFRiskTypeAfterUpsertHooks []TFRiskTypeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TFRiskType) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TFRiskType) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TFRiskType) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TFRiskType) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TFRiskType) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TFRiskType) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TFRiskType) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TFRiskType) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TFRiskType) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tFRiskTypeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTFRiskTypeHook registers your hook function for all future operations.
func AddTFRiskTypeHook(hookPoint boil.HookPoint, tFRiskTypeHook TFRiskTypeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		tFRiskTypeBeforeInsertHooks = append(tFRiskTypeBeforeInsertHooks, tFRiskTypeHook)
	case boil.BeforeUpdateHook:
		tFRiskTypeBeforeUpdateHooks = append(tFRiskTypeBeforeUpdateHooks, tFRiskTypeHook)
	case boil.BeforeDeleteHook:
		tFRiskTypeBeforeDeleteHooks = append(tFRiskTypeBeforeDeleteHooks, tFRiskTypeHook)
	case boil.BeforeUpsertHook:
		tFRiskTypeBeforeUpsertHooks = append(tFRiskTypeBeforeUpsertHooks, tFRiskTypeHook)
	case boil.AfterInsertHook:
		tFRiskTypeAfterInsertHooks = append(tFRiskTypeAfterInsertHooks, tFRiskTypeHook)
	case boil.AfterSelectHook:
		tFRiskTypeAfterSelectHooks = append(tFRiskTypeAfterSelectHooks, tFRiskTypeHook)
	case boil.AfterUpdateHook:
		tFRiskTypeAfterUpdateHooks = append(tFRiskTypeAfterUpdateHooks, tFRiskTypeHook)
	case boil.AfterDeleteHook:
		tFRiskTypeAfterDeleteHooks = append(tFRiskTypeAfterDeleteHooks, tFRiskTypeHook)
	case boil.AfterUpsertHook:
		tFRiskTypeAfterUpsertHooks = append(tFRiskTypeAfterUpsertHooks, tFRiskTypeHook)
	}
}

// One returns a single tFRiskType record from the query.
func (q tFRiskTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TFRiskType, error) {
	o := &TFRiskType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for TFRiskType")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TFRiskType records from the query.
func (q tFRiskTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (TFRiskTypeSlice, error) {
	var o []*TFRiskType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TFRiskType slice")
	}

	if len(tFRiskTypeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TFRiskType records in the query.
func (q tFRiskTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count TFRiskType rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tFRiskTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if TFRiskType exists")
	}

	return count > 0, nil
}

// RiskTypeTFRisks retrieves all the TFRisk's TFRisks with an executor via risk_type_id column.
func (o *TFRiskType) RiskTypeTFRisks(mods ...qm.QueryMod) tFRiskQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`TFRisk`.`risk_type_id`=?", o.RiskTypeID),
	)

	query := TFRisks(queryMods...)
	queries.SetFrom(query.Query, "`TFRisk`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`TFRisk`.*"})
	}

	return query
}

// LoadRiskTypeTFRisks allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tFRiskTypeL) LoadRiskTypeTFRisks(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTFRiskType interface{}, mods queries.Applicator) error {
	var slice []*TFRiskType
	var object *TFRiskType

	if singular {
		object = maybeTFRiskType.(*TFRiskType)
	} else {
		slice = *maybeTFRiskType.(*[]*TFRiskType)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &tFRiskTypeR{}
		}
		args = append(args, object.RiskTypeID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tFRiskTypeR{}
			}

			for _, a := range args {
				if a == obj.RiskTypeID {
					continue Outer
				}
			}

			args = append(args, obj.RiskTypeID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`TFRisk`), qm.WhereIn(`risk_type_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TFRisk")
	}

	var resultSlice []*TFRisk
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TFRisk")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on TFRisk")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for TFRisk")
	}

	if len(tFRiskAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RiskTypeTFRisks = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tFRiskR{}
			}
			foreign.R.RiskType = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.RiskTypeID == foreign.RiskTypeID {
				local.R.RiskTypeTFRisks = append(local.R.RiskTypeTFRisks, foreign)
				if foreign.R == nil {
					foreign.R = &tFRiskR{}
				}
				foreign.R.RiskType = local
				break
			}
		}
	}

	return nil
}

// AddRiskTypeTFRisks adds the given related objects to the existing relationships
// of the TFRiskType, optionally inserting them as new records.
// Appends related to o.R.RiskTypeTFRisks.
// Sets related.R.RiskType appropriately.
func (o *TFRiskType) AddRiskTypeTFRisks(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TFRisk) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.RiskTypeID = o.RiskTypeID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `TFRisk` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"risk_type_id"}),
				strmangle.WhereClause("`", "`", 0, tFRiskPrimaryKeyColumns),
			)
			values := []interface{}{o.RiskTypeID, rel.RiskID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.RiskTypeID = o.RiskTypeID
		}
	}

	if o.R == nil {
		o.R = &tFRiskTypeR{
			RiskTypeTFRisks: related,
		}
	} else {
		o.R.RiskTypeTFRisks = append(o.R.RiskTypeTFRisks, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tFRiskR{
				RiskType: o,
			}
		} else {
			rel.R.RiskType = o
		}
	}
	return nil
}

// TFRiskTypes retrieves all the records using an executor.
func TFRiskTypes(mods ...qm.QueryMod) tFRiskTypeQuery {
	mods = append(mods, qm.From("`TFRiskType`"))
	return tFRiskTypeQuery{NewQuery(mods...)}
}

// FindTFRiskType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTFRiskType(ctx context.Context, exec boil.ContextExecutor, riskTypeID int, selectCols ...string) (*TFRiskType, error) {
	tFRiskTypeObj := &TFRiskType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `TFRiskType` where `risk_type_id`=?", sel,
	)

	q := queries.Raw(query, riskTypeID)

	err := q.Bind(ctx, exec, tFRiskTypeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from TFRiskType")
	}

	return tFRiskTypeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TFRiskType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no TFRiskType provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tFRiskTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tFRiskTypeInsertCacheMut.RLock()
	cache, cached := tFRiskTypeInsertCache[key]
	tFRiskTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tFRiskTypeColumns,
			tFRiskTypeColumnsWithDefault,
			tFRiskTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `TFRiskType` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `TFRiskType` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `TFRiskType` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tFRiskTypePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into TFRiskType")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.RiskTypeID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for TFRiskType")
	}

CacheNoHooks:
	if !cached {
		tFRiskTypeInsertCacheMut.Lock()
		tFRiskTypeInsertCache[key] = cache
		tFRiskTypeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TFRiskType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TFRiskType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tFRiskTypeUpdateCacheMut.RLock()
	cache, cached := tFRiskTypeUpdateCache[key]
	tFRiskTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tFRiskTypeColumns,
			tFRiskTypePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update TFRiskType, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `TFRiskType` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tFRiskTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, append(wl, tFRiskTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update TFRiskType row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for TFRiskType")
	}

	if !cached {
		tFRiskTypeUpdateCacheMut.Lock()
		tFRiskTypeUpdateCache[key] = cache
		tFRiskTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tFRiskTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for TFRiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for TFRiskType")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TFRiskTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tFRiskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `TFRiskType` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tFRiskTypePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tFRiskType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tFRiskType")
	}
	return rowsAff, nil
}

var mySQLTFRiskTypeUniqueColumns = []string{
	"risk_type_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TFRiskType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no TFRiskType provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tFRiskTypeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTFRiskTypeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tFRiskTypeUpsertCacheMut.RLock()
	cache, cached := tFRiskTypeUpsertCache[key]
	tFRiskTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			tFRiskTypeColumns,
			tFRiskTypeColumnsWithDefault,
			tFRiskTypeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			tFRiskTypeColumns,
			tFRiskTypePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert TFRiskType, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "TFRiskType", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `TFRiskType` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for TFRiskType")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tFRiskTypeType, tFRiskTypeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for TFRiskType")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for TFRiskType")
	}

CacheNoHooks:
	if !cached {
		tFRiskTypeUpsertCacheMut.Lock()
		tFRiskTypeUpsertCache[key] = cache
		tFRiskTypeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TFRiskType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TFRiskType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TFRiskType provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tFRiskTypePrimaryKeyMapping)
	sql := "DELETE FROM `TFRiskType` WHERE `risk_type_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from TFRiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for TFRiskType")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tFRiskTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tFRiskTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from TFRiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for TFRiskType")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TFRiskTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TFRiskType slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(tFRiskTypeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tFRiskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `TFRiskType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tFRiskTypePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tFRiskType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for TFRiskType")
	}

	if len(tFRiskTypeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TFRiskType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTFRiskType(ctx, exec, o.RiskTypeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TFRiskTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TFRiskTypeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tFRiskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `TFRiskType`.* FROM `TFRiskType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tFRiskTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TFRiskTypeSlice")
	}

	*o = slice

	return nil
}

// TFRiskTypeExists checks if the TFRiskType row exists.
func TFRiskTypeExists(ctx context.Context, exec boil.ContextExecutor, riskTypeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `TFRiskType` where `risk_type_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, riskTypeID)
	}

	row := exec.QueryRowContext(ctx, sql, riskTypeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if TFRiskType exists")
	}

	return exists, nil
}

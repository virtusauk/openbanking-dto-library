// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// JointAccount is an object representing the database table.
type JointAccount struct {
	AccountID      int         `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	PrimaryPartyID int         `boil:"primary_party_id" json:"primary_party_id" toml:"primary_party_id" yaml:"primary_party_id"`
	LinkedPartyID  int         `boil:"linked_party_id" json:"linked_party_id" toml:"linked_party_id" yaml:"linked_party_id"`
	Relationship   null.String `boil:"relationship" json:"relationship,omitempty" toml:"relationship" yaml:"relationship,omitempty"`
	LinkageType    null.String `boil:"linkage_type" json:"linkage_type,omitempty" toml:"linkage_type" yaml:"linkage_type,omitempty"`
	BankID         int         `boil:"bank_id" json:"bank_id" toml:"bank_id" yaml:"bank_id"`
	MakerDate      time.Time   `boil:"maker_date" json:"maker_date" toml:"maker_date" yaml:"maker_date"`
	CheckerDate    null.Time   `boil:"checker_date" json:"checker_date,omitempty" toml:"checker_date" yaml:"checker_date,omitempty"`
	MakerID        string      `boil:"maker_id" json:"maker_id" toml:"maker_id" yaml:"maker_id"`
	CheckerID      null.String `boil:"checker_id" json:"checker_id,omitempty" toml:"checker_id" yaml:"checker_id,omitempty"`
	ModifiedBy     null.String `boil:"modified_by" json:"modified_by,omitempty" toml:"modified_by" yaml:"modified_by,omitempty"`
	ModifiedDate   null.Time   `boil:"modified_date" json:"modified_date,omitempty" toml:"modified_date" yaml:"modified_date,omitempty"`

	R *jointAccountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L jointAccountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var JointAccountColumns = struct {
	AccountID      string
	PrimaryPartyID string
	LinkedPartyID  string
	Relationship   string
	LinkageType    string
	BankID         string
	MakerDate      string
	CheckerDate    string
	MakerID        string
	CheckerID      string
	ModifiedBy     string
	ModifiedDate   string
}{
	AccountID:      "account_id",
	PrimaryPartyID: "primary_party_id",
	LinkedPartyID:  "linked_party_id",
	Relationship:   "relationship",
	LinkageType:    "linkage_type",
	BankID:         "bank_id",
	MakerDate:      "maker_date",
	CheckerDate:    "checker_date",
	MakerID:        "maker_id",
	CheckerID:      "checker_id",
	ModifiedBy:     "modified_by",
	ModifiedDate:   "modified_date",
}

// Generated where

var JointAccountWhere = struct {
	AccountID      whereHelperint
	PrimaryPartyID whereHelperint
	LinkedPartyID  whereHelperint
	Relationship   whereHelpernull_String
	LinkageType    whereHelpernull_String
	BankID         whereHelperint
	MakerDate      whereHelpertime_Time
	CheckerDate    whereHelpernull_Time
	MakerID        whereHelperstring
	CheckerID      whereHelpernull_String
	ModifiedBy     whereHelpernull_String
	ModifiedDate   whereHelpernull_Time
}{
	AccountID:      whereHelperint{field: `account_id`},
	PrimaryPartyID: whereHelperint{field: `primary_party_id`},
	LinkedPartyID:  whereHelperint{field: `linked_party_id`},
	Relationship:   whereHelpernull_String{field: `relationship`},
	LinkageType:    whereHelpernull_String{field: `linkage_type`},
	BankID:         whereHelperint{field: `bank_id`},
	MakerDate:      whereHelpertime_Time{field: `maker_date`},
	CheckerDate:    whereHelpernull_Time{field: `checker_date`},
	MakerID:        whereHelperstring{field: `maker_id`},
	CheckerID:      whereHelpernull_String{field: `checker_id`},
	ModifiedBy:     whereHelpernull_String{field: `modified_by`},
	ModifiedDate:   whereHelpernull_Time{field: `modified_date`},
}

// JointAccountRels is where relationship names are stored.
var JointAccountRels = struct {
	Bank         string
	LinkedParty  string
	PrimaryParty string
}{
	Bank:         "Bank",
	LinkedParty:  "LinkedParty",
	PrimaryParty: "PrimaryParty",
}

// jointAccountR is where relationships are stored.
type jointAccountR struct {
	Bank         *Bank
	LinkedParty  *Party
	PrimaryParty *Party
}

// NewStruct creates a new relationship struct
func (*jointAccountR) NewStruct() *jointAccountR {
	return &jointAccountR{}
}

// jointAccountL is where Load methods for each relationship are stored.
type jointAccountL struct{}

var (
	jointAccountColumns               = []string{"account_id", "primary_party_id", "linked_party_id", "relationship", "linkage_type", "bank_id", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	jointAccountColumnsWithoutDefault = []string{"account_id", "primary_party_id", "linked_party_id", "relationship", "linkage_type", "bank_id", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	jointAccountColumnsWithDefault    = []string{}
	jointAccountPrimaryKeyColumns     = []string{"account_id"}
)

type (
	// JointAccountSlice is an alias for a slice of pointers to JointAccount.
	// This should generally be used opposed to []JointAccount.
	JointAccountSlice []*JointAccount
	// JointAccountHook is the signature for custom JointAccount hook methods
	JointAccountHook func(context.Context, boil.ContextExecutor, *JointAccount) error

	jointAccountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	jointAccountType                 = reflect.TypeOf(&JointAccount{})
	jointAccountMapping              = queries.MakeStructMapping(jointAccountType)
	jointAccountPrimaryKeyMapping, _ = queries.BindMapping(jointAccountType, jointAccountMapping, jointAccountPrimaryKeyColumns)
	jointAccountInsertCacheMut       sync.RWMutex
	jointAccountInsertCache          = make(map[string]insertCache)
	jointAccountUpdateCacheMut       sync.RWMutex
	jointAccountUpdateCache          = make(map[string]updateCache)
	jointAccountUpsertCacheMut       sync.RWMutex
	jointAccountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var jointAccountBeforeInsertHooks []JointAccountHook
var jointAccountBeforeUpdateHooks []JointAccountHook
var jointAccountBeforeDeleteHooks []JointAccountHook
var jointAccountBeforeUpsertHooks []JointAccountHook

var jointAccountAfterInsertHooks []JointAccountHook
var jointAccountAfterSelectHooks []JointAccountHook
var jointAccountAfterUpdateHooks []JointAccountHook
var jointAccountAfterDeleteHooks []JointAccountHook
var jointAccountAfterUpsertHooks []JointAccountHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *JointAccount) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *JointAccount) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *JointAccount) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *JointAccount) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *JointAccount) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *JointAccount) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *JointAccount) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *JointAccount) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *JointAccount) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range jointAccountAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddJointAccountHook registers your hook function for all future operations.
func AddJointAccountHook(hookPoint boil.HookPoint, jointAccountHook JointAccountHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		jointAccountBeforeInsertHooks = append(jointAccountBeforeInsertHooks, jointAccountHook)
	case boil.BeforeUpdateHook:
		jointAccountBeforeUpdateHooks = append(jointAccountBeforeUpdateHooks, jointAccountHook)
	case boil.BeforeDeleteHook:
		jointAccountBeforeDeleteHooks = append(jointAccountBeforeDeleteHooks, jointAccountHook)
	case boil.BeforeUpsertHook:
		jointAccountBeforeUpsertHooks = append(jointAccountBeforeUpsertHooks, jointAccountHook)
	case boil.AfterInsertHook:
		jointAccountAfterInsertHooks = append(jointAccountAfterInsertHooks, jointAccountHook)
	case boil.AfterSelectHook:
		jointAccountAfterSelectHooks = append(jointAccountAfterSelectHooks, jointAccountHook)
	case boil.AfterUpdateHook:
		jointAccountAfterUpdateHooks = append(jointAccountAfterUpdateHooks, jointAccountHook)
	case boil.AfterDeleteHook:
		jointAccountAfterDeleteHooks = append(jointAccountAfterDeleteHooks, jointAccountHook)
	case boil.AfterUpsertHook:
		jointAccountAfterUpsertHooks = append(jointAccountAfterUpsertHooks, jointAccountHook)
	}
}

// One returns a single jointAccount record from the query.
func (q jointAccountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*JointAccount, error) {
	o := &JointAccount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for JointAccount")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all JointAccount records from the query.
func (q jointAccountQuery) All(ctx context.Context, exec boil.ContextExecutor) (JointAccountSlice, error) {
	var o []*JointAccount

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to JointAccount slice")
	}

	if len(jointAccountAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all JointAccount records in the query.
func (q jointAccountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count JointAccount rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q jointAccountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if JointAccount exists")
	}

	return count > 0, nil
}

// Bank pointed to by the foreign key.
func (o *JointAccount) Bank(mods ...qm.QueryMod) bankQuery {
	queryMods := []qm.QueryMod{
		qm.Where("bank_id=?", o.BankID),
	}

	queryMods = append(queryMods, mods...)

	query := Banks(queryMods...)
	queries.SetFrom(query.Query, "`Banks`")

	return query
}

// LinkedParty pointed to by the foreign key.
func (o *JointAccount) LinkedParty(mods ...qm.QueryMod) partyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("party_id=?", o.LinkedPartyID),
	}

	queryMods = append(queryMods, mods...)

	query := Parties(queryMods...)
	queries.SetFrom(query.Query, "`Parties`")

	return query
}

// PrimaryParty pointed to by the foreign key.
func (o *JointAccount) PrimaryParty(mods ...qm.QueryMod) partyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("party_id=?", o.PrimaryPartyID),
	}

	queryMods = append(queryMods, mods...)

	query := Parties(queryMods...)
	queries.SetFrom(query.Query, "`Parties`")

	return query
}

// LoadBank allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (jointAccountL) LoadBank(ctx context.Context, e boil.ContextExecutor, singular bool, maybeJointAccount interface{}, mods queries.Applicator) error {
	var slice []*JointAccount
	var object *JointAccount

	if singular {
		object = maybeJointAccount.(*JointAccount)
	} else {
		slice = *maybeJointAccount.(*[]*JointAccount)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &jointAccountR{}
		}
		args = append(args, object.BankID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &jointAccountR{}
			}

			for _, a := range args {
				if a == obj.BankID {
					continue Outer
				}
			}

			args = append(args, obj.BankID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Banks`), qm.WhereIn(`bank_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Bank")
	}

	var resultSlice []*Bank
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Bank")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Banks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Banks")
	}

	if len(jointAccountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Bank = foreign
		if foreign.R == nil {
			foreign.R = &bankR{}
		}
		foreign.R.BankJointAccounts = append(foreign.R.BankJointAccounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BankID == foreign.BankID {
				local.R.Bank = foreign
				if foreign.R == nil {
					foreign.R = &bankR{}
				}
				foreign.R.BankJointAccounts = append(foreign.R.BankJointAccounts, local)
				break
			}
		}
	}

	return nil
}

// LoadLinkedParty allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (jointAccountL) LoadLinkedParty(ctx context.Context, e boil.ContextExecutor, singular bool, maybeJointAccount interface{}, mods queries.Applicator) error {
	var slice []*JointAccount
	var object *JointAccount

	if singular {
		object = maybeJointAccount.(*JointAccount)
	} else {
		slice = *maybeJointAccount.(*[]*JointAccount)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &jointAccountR{}
		}
		args = append(args, object.LinkedPartyID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &jointAccountR{}
			}

			for _, a := range args {
				if a == obj.LinkedPartyID {
					continue Outer
				}
			}

			args = append(args, obj.LinkedPartyID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Parties`), qm.WhereIn(`party_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Party")
	}

	var resultSlice []*Party
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Party")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Parties")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Parties")
	}

	if len(jointAccountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.LinkedParty = foreign
		if foreign.R == nil {
			foreign.R = &partyR{}
		}
		foreign.R.LinkedPartyJointAccounts = append(foreign.R.LinkedPartyJointAccounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LinkedPartyID == foreign.PartyID {
				local.R.LinkedParty = foreign
				if foreign.R == nil {
					foreign.R = &partyR{}
				}
				foreign.R.LinkedPartyJointAccounts = append(foreign.R.LinkedPartyJointAccounts, local)
				break
			}
		}
	}

	return nil
}

// LoadPrimaryParty allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (jointAccountL) LoadPrimaryParty(ctx context.Context, e boil.ContextExecutor, singular bool, maybeJointAccount interface{}, mods queries.Applicator) error {
	var slice []*JointAccount
	var object *JointAccount

	if singular {
		object = maybeJointAccount.(*JointAccount)
	} else {
		slice = *maybeJointAccount.(*[]*JointAccount)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &jointAccountR{}
		}
		args = append(args, object.PrimaryPartyID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &jointAccountR{}
			}

			for _, a := range args {
				if a == obj.PrimaryPartyID {
					continue Outer
				}
			}

			args = append(args, obj.PrimaryPartyID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Parties`), qm.WhereIn(`party_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Party")
	}

	var resultSlice []*Party
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Party")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Parties")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Parties")
	}

	if len(jointAccountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PrimaryParty = foreign
		if foreign.R == nil {
			foreign.R = &partyR{}
		}
		foreign.R.PrimaryPartyJointAccounts = append(foreign.R.PrimaryPartyJointAccounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PrimaryPartyID == foreign.PartyID {
				local.R.PrimaryParty = foreign
				if foreign.R == nil {
					foreign.R = &partyR{}
				}
				foreign.R.PrimaryPartyJointAccounts = append(foreign.R.PrimaryPartyJointAccounts, local)
				break
			}
		}
	}

	return nil
}

// SetBank of the jointAccount to the related item.
// Sets o.R.Bank to related.
// Adds o to related.R.BankJointAccounts.
func (o *JointAccount) SetBank(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Bank) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `JointAccount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"bank_id"}),
		strmangle.WhereClause("`", "`", 0, jointAccountPrimaryKeyColumns),
	)
	values := []interface{}{related.BankID, o.AccountID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BankID = related.BankID
	if o.R == nil {
		o.R = &jointAccountR{
			Bank: related,
		}
	} else {
		o.R.Bank = related
	}

	if related.R == nil {
		related.R = &bankR{
			BankJointAccounts: JointAccountSlice{o},
		}
	} else {
		related.R.BankJointAccounts = append(related.R.BankJointAccounts, o)
	}

	return nil
}

// SetLinkedParty of the jointAccount to the related item.
// Sets o.R.LinkedParty to related.
// Adds o to related.R.LinkedPartyJointAccounts.
func (o *JointAccount) SetLinkedParty(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Party) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `JointAccount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"linked_party_id"}),
		strmangle.WhereClause("`", "`", 0, jointAccountPrimaryKeyColumns),
	)
	values := []interface{}{related.PartyID, o.AccountID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LinkedPartyID = related.PartyID
	if o.R == nil {
		o.R = &jointAccountR{
			LinkedParty: related,
		}
	} else {
		o.R.LinkedParty = related
	}

	if related.R == nil {
		related.R = &partyR{
			LinkedPartyJointAccounts: JointAccountSlice{o},
		}
	} else {
		related.R.LinkedPartyJointAccounts = append(related.R.LinkedPartyJointAccounts, o)
	}

	return nil
}

// SetPrimaryParty of the jointAccount to the related item.
// Sets o.R.PrimaryParty to related.
// Adds o to related.R.PrimaryPartyJointAccounts.
func (o *JointAccount) SetPrimaryParty(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Party) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `JointAccount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"primary_party_id"}),
		strmangle.WhereClause("`", "`", 0, jointAccountPrimaryKeyColumns),
	)
	values := []interface{}{related.PartyID, o.AccountID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PrimaryPartyID = related.PartyID
	if o.R == nil {
		o.R = &jointAccountR{
			PrimaryParty: related,
		}
	} else {
		o.R.PrimaryParty = related
	}

	if related.R == nil {
		related.R = &partyR{
			PrimaryPartyJointAccounts: JointAccountSlice{o},
		}
	} else {
		related.R.PrimaryPartyJointAccounts = append(related.R.PrimaryPartyJointAccounts, o)
	}

	return nil
}

// JointAccounts retrieves all the records using an executor.
func JointAccounts(mods ...qm.QueryMod) jointAccountQuery {
	mods = append(mods, qm.From("`JointAccount`"))
	return jointAccountQuery{NewQuery(mods...)}
}

// FindJointAccount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindJointAccount(ctx context.Context, exec boil.ContextExecutor, accountID int, selectCols ...string) (*JointAccount, error) {
	jointAccountObj := &JointAccount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `JointAccount` where `account_id`=?", sel,
	)

	q := queries.Raw(query, accountID)

	err := q.Bind(ctx, exec, jointAccountObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from JointAccount")
	}

	return jointAccountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *JointAccount) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JointAccount provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(jointAccountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	jointAccountInsertCacheMut.RLock()
	cache, cached := jointAccountInsertCache[key]
	jointAccountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			jointAccountColumns,
			jointAccountColumnsWithDefault,
			jointAccountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(jointAccountType, jointAccountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(jointAccountType, jointAccountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `JointAccount` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `JointAccount` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `JointAccount` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, jointAccountPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into JointAccount")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.AccountID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for JointAccount")
	}

CacheNoHooks:
	if !cached {
		jointAccountInsertCacheMut.Lock()
		jointAccountInsertCache[key] = cache
		jointAccountInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the JointAccount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *JointAccount) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	jointAccountUpdateCacheMut.RLock()
	cache, cached := jointAccountUpdateCache[key]
	jointAccountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			jointAccountColumns,
			jointAccountPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update JointAccount, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `JointAccount` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, jointAccountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(jointAccountType, jointAccountMapping, append(wl, jointAccountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update JointAccount row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for JointAccount")
	}

	if !cached {
		jointAccountUpdateCacheMut.Lock()
		jointAccountUpdateCache[key] = cache
		jointAccountUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q jointAccountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for JointAccount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for JointAccount")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o JointAccountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), jointAccountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `JointAccount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, jointAccountPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in jointAccount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all jointAccount")
	}
	return rowsAff, nil
}

var mySQLJointAccountUniqueColumns = []string{
	"account_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *JointAccount) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no JointAccount provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(jointAccountColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLJointAccountUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	jointAccountUpsertCacheMut.RLock()
	cache, cached := jointAccountUpsertCache[key]
	jointAccountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			jointAccountColumns,
			jointAccountColumnsWithDefault,
			jointAccountColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			jointAccountColumns,
			jointAccountPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert JointAccount, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "JointAccount", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `JointAccount` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(jointAccountType, jointAccountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(jointAccountType, jointAccountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for JointAccount")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(jointAccountType, jointAccountMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for JointAccount")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for JointAccount")
	}

CacheNoHooks:
	if !cached {
		jointAccountUpsertCacheMut.Lock()
		jointAccountUpsertCache[key] = cache
		jointAccountUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single JointAccount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *JointAccount) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no JointAccount provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), jointAccountPrimaryKeyMapping)
	sql := "DELETE FROM `JointAccount` WHERE `account_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from JointAccount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for JointAccount")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q jointAccountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no jointAccountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from JointAccount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JointAccount")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o JointAccountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no JointAccount slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(jointAccountBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), jointAccountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `JointAccount` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, jointAccountPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from jointAccount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for JointAccount")
	}

	if len(jointAccountAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *JointAccount) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindJointAccount(ctx, exec, o.AccountID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *JointAccountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := JointAccountSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), jointAccountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `JointAccount`.* FROM `JointAccount` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, jointAccountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in JointAccountSlice")
	}

	*o = slice

	return nil
}

// JointAccountExists checks if the JointAccount row exists.
func JointAccountExists(ctx context.Context, exec boil.ContextExecutor, accountID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `JointAccount` where `account_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, accountID)
	}

	row := exec.QueryRowContext(ctx, sql, accountID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if JointAccount exists")
	}

	return exists, nil
}

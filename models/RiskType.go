// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// RiskType is an object representing the database table.
type RiskType struct {
	RiskTypeID   int         `boil:"risk_type_id" json:"risk_type_id" toml:"risk_type_id" yaml:"risk_type_id"`
	RiskType     string      `boil:"risk_type" json:"risk_type" toml:"risk_type" yaml:"risk_type"`
	Description  string      `boil:"description" json:"description" toml:"description" yaml:"description"`
	MakerDate    time.Time   `boil:"maker_date" json:"maker_date" toml:"maker_date" yaml:"maker_date"`
	CheckerDate  null.Time   `boil:"checker_date" json:"checker_date,omitempty" toml:"checker_date" yaml:"checker_date,omitempty"`
	MakerID      string      `boil:"maker_id" json:"maker_id" toml:"maker_id" yaml:"maker_id"`
	CheckerID    null.String `boil:"checker_id" json:"checker_id,omitempty" toml:"checker_id" yaml:"checker_id,omitempty"`
	ModifiedBy   null.String `boil:"modified_by" json:"modified_by,omitempty" toml:"modified_by" yaml:"modified_by,omitempty"`
	ModifiedDate null.Time   `boil:"modified_date" json:"modified_date,omitempty" toml:"modified_date" yaml:"modified_date,omitempty"`

	R *riskTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L riskTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RiskTypeColumns = struct {
	RiskTypeID   string
	RiskType     string
	Description  string
	MakerDate    string
	CheckerDate  string
	MakerID      string
	CheckerID    string
	ModifiedBy   string
	ModifiedDate string
}{
	RiskTypeID:   "risk_type_id",
	RiskType:     "risk_type",
	Description:  "description",
	MakerDate:    "maker_date",
	CheckerDate:  "checker_date",
	MakerID:      "maker_id",
	CheckerID:    "checker_id",
	ModifiedBy:   "modified_by",
	ModifiedDate: "modified_date",
}

// Generated where

var RiskTypeWhere = struct {
	RiskTypeID   whereHelperint
	RiskType     whereHelperstring
	Description  whereHelperstring
	MakerDate    whereHelpertime_Time
	CheckerDate  whereHelpernull_Time
	MakerID      whereHelperstring
	CheckerID    whereHelpernull_String
	ModifiedBy   whereHelpernull_String
	ModifiedDate whereHelpernull_Time
}{
	RiskTypeID:   whereHelperint{field: `risk_type_id`},
	RiskType:     whereHelperstring{field: `risk_type`},
	Description:  whereHelperstring{field: `description`},
	MakerDate:    whereHelpertime_Time{field: `maker_date`},
	CheckerDate:  whereHelpernull_Time{field: `checker_date`},
	MakerID:      whereHelperstring{field: `maker_id`},
	CheckerID:    whereHelpernull_String{field: `checker_id`},
	ModifiedBy:   whereHelpernull_String{field: `modified_by`},
	ModifiedDate: whereHelpernull_Time{field: `modified_date`},
}

// RiskTypeRels is where relationship names are stored.
var RiskTypeRels = struct {
	RiskTypeRisks string
}{
	RiskTypeRisks: "RiskTypeRisks",
}

// riskTypeR is where relationships are stored.
type riskTypeR struct {
	RiskTypeRisks RiskSlice
}

// NewStruct creates a new relationship struct
func (*riskTypeR) NewStruct() *riskTypeR {
	return &riskTypeR{}
}

// riskTypeL is where Load methods for each relationship are stored.
type riskTypeL struct{}

var (
	riskTypeColumns               = []string{"risk_type_id", "risk_type", "description", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	riskTypeColumnsWithoutDefault = []string{"risk_type", "description", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	riskTypeColumnsWithDefault    = []string{"risk_type_id"}
	riskTypePrimaryKeyColumns     = []string{"risk_type_id"}
)

type (
	// RiskTypeSlice is an alias for a slice of pointers to RiskType.
	// This should generally be used opposed to []RiskType.
	RiskTypeSlice []*RiskType
	// RiskTypeHook is the signature for custom RiskType hook methods
	RiskTypeHook func(context.Context, boil.ContextExecutor, *RiskType) error

	riskTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	riskTypeType                 = reflect.TypeOf(&RiskType{})
	riskTypeMapping              = queries.MakeStructMapping(riskTypeType)
	riskTypePrimaryKeyMapping, _ = queries.BindMapping(riskTypeType, riskTypeMapping, riskTypePrimaryKeyColumns)
	riskTypeInsertCacheMut       sync.RWMutex
	riskTypeInsertCache          = make(map[string]insertCache)
	riskTypeUpdateCacheMut       sync.RWMutex
	riskTypeUpdateCache          = make(map[string]updateCache)
	riskTypeUpsertCacheMut       sync.RWMutex
	riskTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var riskTypeBeforeInsertHooks []RiskTypeHook
var riskTypeBeforeUpdateHooks []RiskTypeHook
var riskTypeBeforeDeleteHooks []RiskTypeHook
var riskTypeBeforeUpsertHooks []RiskTypeHook

var riskTypeAfterInsertHooks []RiskTypeHook
var riskTypeAfterSelectHooks []RiskTypeHook
var riskTypeAfterUpdateHooks []RiskTypeHook
var riskTypeAfterDeleteHooks []RiskTypeHook
var riskTypeAfterUpsertHooks []RiskTypeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RiskType) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RiskType) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RiskType) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RiskType) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RiskType) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RiskType) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RiskType) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RiskType) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RiskType) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range riskTypeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRiskTypeHook registers your hook function for all future operations.
func AddRiskTypeHook(hookPoint boil.HookPoint, riskTypeHook RiskTypeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		riskTypeBeforeInsertHooks = append(riskTypeBeforeInsertHooks, riskTypeHook)
	case boil.BeforeUpdateHook:
		riskTypeBeforeUpdateHooks = append(riskTypeBeforeUpdateHooks, riskTypeHook)
	case boil.BeforeDeleteHook:
		riskTypeBeforeDeleteHooks = append(riskTypeBeforeDeleteHooks, riskTypeHook)
	case boil.BeforeUpsertHook:
		riskTypeBeforeUpsertHooks = append(riskTypeBeforeUpsertHooks, riskTypeHook)
	case boil.AfterInsertHook:
		riskTypeAfterInsertHooks = append(riskTypeAfterInsertHooks, riskTypeHook)
	case boil.AfterSelectHook:
		riskTypeAfterSelectHooks = append(riskTypeAfterSelectHooks, riskTypeHook)
	case boil.AfterUpdateHook:
		riskTypeAfterUpdateHooks = append(riskTypeAfterUpdateHooks, riskTypeHook)
	case boil.AfterDeleteHook:
		riskTypeAfterDeleteHooks = append(riskTypeAfterDeleteHooks, riskTypeHook)
	case boil.AfterUpsertHook:
		riskTypeAfterUpsertHooks = append(riskTypeAfterUpsertHooks, riskTypeHook)
	}
}

// One returns a single riskType record from the query.
func (q riskTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RiskType, error) {
	o := &RiskType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for RiskType")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RiskType records from the query.
func (q riskTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (RiskTypeSlice, error) {
	var o []*RiskType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RiskType slice")
	}

	if len(riskTypeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RiskType records in the query.
func (q riskTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count RiskType rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q riskTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if RiskType exists")
	}

	return count > 0, nil
}

// RiskTypeRisks retrieves all the Risk's Risks with an executor via risk_type_id column.
func (o *RiskType) RiskTypeRisks(mods ...qm.QueryMod) riskQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`Risk`.`risk_type_id`=?", o.RiskTypeID),
	)

	query := Risks(queryMods...)
	queries.SetFrom(query.Query, "`Risk`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`Risk`.*"})
	}

	return query
}

// LoadRiskTypeRisks allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (riskTypeL) LoadRiskTypeRisks(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRiskType interface{}, mods queries.Applicator) error {
	var slice []*RiskType
	var object *RiskType

	if singular {
		object = maybeRiskType.(*RiskType)
	} else {
		slice = *maybeRiskType.(*[]*RiskType)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &riskTypeR{}
		}
		args = append(args, object.RiskTypeID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &riskTypeR{}
			}

			for _, a := range args {
				if a == obj.RiskTypeID {
					continue Outer
				}
			}

			args = append(args, obj.RiskTypeID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Risk`), qm.WhereIn(`risk_type_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Risk")
	}

	var resultSlice []*Risk
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Risk")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on Risk")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Risk")
	}

	if len(riskAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RiskTypeRisks = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &riskR{}
			}
			foreign.R.RiskType = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.RiskTypeID == foreign.RiskTypeID {
				local.R.RiskTypeRisks = append(local.R.RiskTypeRisks, foreign)
				if foreign.R == nil {
					foreign.R = &riskR{}
				}
				foreign.R.RiskType = local
				break
			}
		}
	}

	return nil
}

// AddRiskTypeRisks adds the given related objects to the existing relationships
// of the RiskType, optionally inserting them as new records.
// Appends related to o.R.RiskTypeRisks.
// Sets related.R.RiskType appropriately.
func (o *RiskType) AddRiskTypeRisks(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Risk) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.RiskTypeID = o.RiskTypeID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `Risk` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"risk_type_id"}),
				strmangle.WhereClause("`", "`", 0, riskPrimaryKeyColumns),
			)
			values := []interface{}{o.RiskTypeID, rel.RiskID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.RiskTypeID = o.RiskTypeID
		}
	}

	if o.R == nil {
		o.R = &riskTypeR{
			RiskTypeRisks: related,
		}
	} else {
		o.R.RiskTypeRisks = append(o.R.RiskTypeRisks, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &riskR{
				RiskType: o,
			}
		} else {
			rel.R.RiskType = o
		}
	}
	return nil
}

// RiskTypes retrieves all the records using an executor.
func RiskTypes(mods ...qm.QueryMod) riskTypeQuery {
	mods = append(mods, qm.From("`RiskType`"))
	return riskTypeQuery{NewQuery(mods...)}
}

// FindRiskType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRiskType(ctx context.Context, exec boil.ContextExecutor, riskTypeID int, selectCols ...string) (*RiskType, error) {
	riskTypeObj := &RiskType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `RiskType` where `risk_type_id`=?", sel,
	)

	q := queries.Raw(query, riskTypeID)

	err := q.Bind(ctx, exec, riskTypeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from RiskType")
	}

	return riskTypeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RiskType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no RiskType provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(riskTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	riskTypeInsertCacheMut.RLock()
	cache, cached := riskTypeInsertCache[key]
	riskTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			riskTypeColumns,
			riskTypeColumnsWithDefault,
			riskTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(riskTypeType, riskTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(riskTypeType, riskTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `RiskType` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `RiskType` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `RiskType` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, riskTypePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into RiskType")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.RiskTypeID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == riskTypeMapping["RiskTypeID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.RiskTypeID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for RiskType")
	}

CacheNoHooks:
	if !cached {
		riskTypeInsertCacheMut.Lock()
		riskTypeInsertCache[key] = cache
		riskTypeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RiskType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RiskType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	riskTypeUpdateCacheMut.RLock()
	cache, cached := riskTypeUpdateCache[key]
	riskTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			riskTypeColumns,
			riskTypePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update RiskType, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `RiskType` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, riskTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(riskTypeType, riskTypeMapping, append(wl, riskTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update RiskType row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for RiskType")
	}

	if !cached {
		riskTypeUpdateCacheMut.Lock()
		riskTypeUpdateCache[key] = cache
		riskTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q riskTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for RiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for RiskType")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RiskTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), riskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `RiskType` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, riskTypePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in riskType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all riskType")
	}
	return rowsAff, nil
}

var mySQLRiskTypeUniqueColumns = []string{
	"risk_type_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RiskType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no RiskType provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(riskTypeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRiskTypeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	riskTypeUpsertCacheMut.RLock()
	cache, cached := riskTypeUpsertCache[key]
	riskTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			riskTypeColumns,
			riskTypeColumnsWithDefault,
			riskTypeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			riskTypeColumns,
			riskTypePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert RiskType, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "RiskType", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `RiskType` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(riskTypeType, riskTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(riskTypeType, riskTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for RiskType")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.RiskTypeID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == riskTypeMapping["risk_type_id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(riskTypeType, riskTypeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for RiskType")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for RiskType")
	}

CacheNoHooks:
	if !cached {
		riskTypeUpsertCacheMut.Lock()
		riskTypeUpsertCache[key] = cache
		riskTypeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RiskType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RiskType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RiskType provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), riskTypePrimaryKeyMapping)
	sql := "DELETE FROM `RiskType` WHERE `risk_type_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from RiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for RiskType")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q riskTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no riskTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from RiskType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for RiskType")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RiskTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RiskType slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(riskTypeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), riskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `RiskType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, riskTypePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from riskType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for RiskType")
	}

	if len(riskTypeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RiskType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRiskType(ctx, exec, o.RiskTypeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RiskTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RiskTypeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), riskTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `RiskType`.* FROM `RiskType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, riskTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RiskTypeSlice")
	}

	*o = slice

	return nil
}

// RiskTypeExists checks if the RiskType row exists.
func RiskTypeExists(ctx context.Context, exec boil.ContextExecutor, riskTypeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `RiskType` where `risk_type_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, riskTypeID)
	}

	row := exec.QueryRowContext(ctx, sql, riskTypeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if RiskType exists")
	}

	return exists, nil
}

// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// CustomerRating is an object representing the database table.
type CustomerRating struct {
	CustomerRatingID int         `boil:"customer_rating_id" json:"customer_rating_id" toml:"customer_rating_id" yaml:"customer_rating_id"`
	PartyID          int         `boil:"party_id" json:"party_id" toml:"party_id" yaml:"party_id"`
	RatingDate       time.Time   `boil:"rating_date" json:"rating_date" toml:"rating_date" yaml:"rating_date"`
	CustomerRating   null.String `boil:"customer_rating" json:"customer_rating,omitempty" toml:"customer_rating" yaml:"customer_rating,omitempty"`
	Description      null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	StartDate        null.Time   `boil:"start_date" json:"start_date,omitempty" toml:"start_date" yaml:"start_date,omitempty"`
	EndDate          null.Time   `boil:"end_date" json:"end_date,omitempty" toml:"end_date" yaml:"end_date,omitempty"`
	BankID           int         `boil:"bank_id" json:"bank_id" toml:"bank_id" yaml:"bank_id"`
	MakerDate        time.Time   `boil:"maker_date" json:"maker_date" toml:"maker_date" yaml:"maker_date"`
	CheckerDate      null.Time   `boil:"checker_date" json:"checker_date,omitempty" toml:"checker_date" yaml:"checker_date,omitempty"`
	MakerID          string      `boil:"maker_id" json:"maker_id" toml:"maker_id" yaml:"maker_id"`
	CheckerID        null.String `boil:"checker_id" json:"checker_id,omitempty" toml:"checker_id" yaml:"checker_id,omitempty"`
	ModifiedBy       null.String `boil:"modified_by" json:"modified_by,omitempty" toml:"modified_by" yaml:"modified_by,omitempty"`
	ModifiedDate     null.Time   `boil:"modified_date" json:"modified_date,omitempty" toml:"modified_date" yaml:"modified_date,omitempty"`

	R *customerRatingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L customerRatingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CustomerRatingColumns = struct {
	CustomerRatingID string
	PartyID          string
	RatingDate       string
	CustomerRating   string
	Description      string
	StartDate        string
	EndDate          string
	BankID           string
	MakerDate        string
	CheckerDate      string
	MakerID          string
	CheckerID        string
	ModifiedBy       string
	ModifiedDate     string
}{
	CustomerRatingID: "customer_rating_id",
	PartyID:          "party_id",
	RatingDate:       "rating_date",
	CustomerRating:   "customer_rating",
	Description:      "description",
	StartDate:        "start_date",
	EndDate:          "end_date",
	BankID:           "bank_id",
	MakerDate:        "maker_date",
	CheckerDate:      "checker_date",
	MakerID:          "maker_id",
	CheckerID:        "checker_id",
	ModifiedBy:       "modified_by",
	ModifiedDate:     "modified_date",
}

// Generated where

var CustomerRatingWhere = struct {
	CustomerRatingID whereHelperint
	PartyID          whereHelperint
	RatingDate       whereHelpertime_Time
	CustomerRating   whereHelpernull_String
	Description      whereHelpernull_String
	StartDate        whereHelpernull_Time
	EndDate          whereHelpernull_Time
	BankID           whereHelperint
	MakerDate        whereHelpertime_Time
	CheckerDate      whereHelpernull_Time
	MakerID          whereHelperstring
	CheckerID        whereHelpernull_String
	ModifiedBy       whereHelpernull_String
	ModifiedDate     whereHelpernull_Time
}{
	CustomerRatingID: whereHelperint{field: `customer_rating_id`},
	PartyID:          whereHelperint{field: `party_id`},
	RatingDate:       whereHelpertime_Time{field: `rating_date`},
	CustomerRating:   whereHelpernull_String{field: `customer_rating`},
	Description:      whereHelpernull_String{field: `description`},
	StartDate:        whereHelpernull_Time{field: `start_date`},
	EndDate:          whereHelpernull_Time{field: `end_date`},
	BankID:           whereHelperint{field: `bank_id`},
	MakerDate:        whereHelpertime_Time{field: `maker_date`},
	CheckerDate:      whereHelpernull_Time{field: `checker_date`},
	MakerID:          whereHelperstring{field: `maker_id`},
	CheckerID:        whereHelpernull_String{field: `checker_id`},
	ModifiedBy:       whereHelpernull_String{field: `modified_by`},
	ModifiedDate:     whereHelpernull_Time{field: `modified_date`},
}

// CustomerRatingRels is where relationship names are stored.
var CustomerRatingRels = struct {
	Party string
}{
	Party: "Party",
}

// customerRatingR is where relationships are stored.
type customerRatingR struct {
	Party *Party
}

// NewStruct creates a new relationship struct
func (*customerRatingR) NewStruct() *customerRatingR {
	return &customerRatingR{}
}

// customerRatingL is where Load methods for each relationship are stored.
type customerRatingL struct{}

var (
	customerRatingColumns               = []string{"customer_rating_id", "party_id", "rating_date", "customer_rating", "description", "start_date", "end_date", "bank_id", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	customerRatingColumnsWithoutDefault = []string{"customer_rating_id", "rating_date", "customer_rating", "description", "start_date", "end_date", "bank_id", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	customerRatingColumnsWithDefault    = []string{"party_id"}
	customerRatingPrimaryKeyColumns     = []string{"customer_rating_id"}
)

type (
	// CustomerRatingSlice is an alias for a slice of pointers to CustomerRating.
	// This should generally be used opposed to []CustomerRating.
	CustomerRatingSlice []*CustomerRating
	// CustomerRatingHook is the signature for custom CustomerRating hook methods
	CustomerRatingHook func(context.Context, boil.ContextExecutor, *CustomerRating) error

	customerRatingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	customerRatingType                 = reflect.TypeOf(&CustomerRating{})
	customerRatingMapping              = queries.MakeStructMapping(customerRatingType)
	customerRatingPrimaryKeyMapping, _ = queries.BindMapping(customerRatingType, customerRatingMapping, customerRatingPrimaryKeyColumns)
	customerRatingInsertCacheMut       sync.RWMutex
	customerRatingInsertCache          = make(map[string]insertCache)
	customerRatingUpdateCacheMut       sync.RWMutex
	customerRatingUpdateCache          = make(map[string]updateCache)
	customerRatingUpsertCacheMut       sync.RWMutex
	customerRatingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var customerRatingBeforeInsertHooks []CustomerRatingHook
var customerRatingBeforeUpdateHooks []CustomerRatingHook
var customerRatingBeforeDeleteHooks []CustomerRatingHook
var customerRatingBeforeUpsertHooks []CustomerRatingHook

var customerRatingAfterInsertHooks []CustomerRatingHook
var customerRatingAfterSelectHooks []CustomerRatingHook
var customerRatingAfterUpdateHooks []CustomerRatingHook
var customerRatingAfterDeleteHooks []CustomerRatingHook
var customerRatingAfterUpsertHooks []CustomerRatingHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CustomerRating) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CustomerRating) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CustomerRating) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CustomerRating) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CustomerRating) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CustomerRating) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CustomerRating) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CustomerRating) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CustomerRating) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range customerRatingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCustomerRatingHook registers your hook function for all future operations.
func AddCustomerRatingHook(hookPoint boil.HookPoint, customerRatingHook CustomerRatingHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		customerRatingBeforeInsertHooks = append(customerRatingBeforeInsertHooks, customerRatingHook)
	case boil.BeforeUpdateHook:
		customerRatingBeforeUpdateHooks = append(customerRatingBeforeUpdateHooks, customerRatingHook)
	case boil.BeforeDeleteHook:
		customerRatingBeforeDeleteHooks = append(customerRatingBeforeDeleteHooks, customerRatingHook)
	case boil.BeforeUpsertHook:
		customerRatingBeforeUpsertHooks = append(customerRatingBeforeUpsertHooks, customerRatingHook)
	case boil.AfterInsertHook:
		customerRatingAfterInsertHooks = append(customerRatingAfterInsertHooks, customerRatingHook)
	case boil.AfterSelectHook:
		customerRatingAfterSelectHooks = append(customerRatingAfterSelectHooks, customerRatingHook)
	case boil.AfterUpdateHook:
		customerRatingAfterUpdateHooks = append(customerRatingAfterUpdateHooks, customerRatingHook)
	case boil.AfterDeleteHook:
		customerRatingAfterDeleteHooks = append(customerRatingAfterDeleteHooks, customerRatingHook)
	case boil.AfterUpsertHook:
		customerRatingAfterUpsertHooks = append(customerRatingAfterUpsertHooks, customerRatingHook)
	}
}

// One returns a single customerRating record from the query.
func (q customerRatingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CustomerRating, error) {
	o := &CustomerRating{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for CustomerRating")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CustomerRating records from the query.
func (q customerRatingQuery) All(ctx context.Context, exec boil.ContextExecutor) (CustomerRatingSlice, error) {
	var o []*CustomerRating

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CustomerRating slice")
	}

	if len(customerRatingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CustomerRating records in the query.
func (q customerRatingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count CustomerRating rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q customerRatingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if CustomerRating exists")
	}

	return count > 0, nil
}

// Party pointed to by the foreign key.
func (o *CustomerRating) Party(mods ...qm.QueryMod) partyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("party_id=?", o.PartyID),
	}

	queryMods = append(queryMods, mods...)

	query := Parties(queryMods...)
	queries.SetFrom(query.Query, "`Parties`")

	return query
}

// LoadParty allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (customerRatingL) LoadParty(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCustomerRating interface{}, mods queries.Applicator) error {
	var slice []*CustomerRating
	var object *CustomerRating

	if singular {
		object = maybeCustomerRating.(*CustomerRating)
	} else {
		slice = *maybeCustomerRating.(*[]*CustomerRating)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &customerRatingR{}
		}
		args = append(args, object.PartyID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &customerRatingR{}
			}

			for _, a := range args {
				if a == obj.PartyID {
					continue Outer
				}
			}

			args = append(args, obj.PartyID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Parties`), qm.WhereIn(`party_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Party")
	}

	var resultSlice []*Party
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Party")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Parties")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Parties")
	}

	if len(customerRatingAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Party = foreign
		if foreign.R == nil {
			foreign.R = &partyR{}
		}
		foreign.R.PartyCustomerRatings = append(foreign.R.PartyCustomerRatings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PartyID == foreign.PartyID {
				local.R.Party = foreign
				if foreign.R == nil {
					foreign.R = &partyR{}
				}
				foreign.R.PartyCustomerRatings = append(foreign.R.PartyCustomerRatings, local)
				break
			}
		}
	}

	return nil
}

// SetParty of the customerRating to the related item.
// Sets o.R.Party to related.
// Adds o to related.R.PartyCustomerRatings.
func (o *CustomerRating) SetParty(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Party) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `CustomerRating` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"party_id"}),
		strmangle.WhereClause("`", "`", 0, customerRatingPrimaryKeyColumns),
	)
	values := []interface{}{related.PartyID, o.CustomerRatingID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PartyID = related.PartyID
	if o.R == nil {
		o.R = &customerRatingR{
			Party: related,
		}
	} else {
		o.R.Party = related
	}

	if related.R == nil {
		related.R = &partyR{
			PartyCustomerRatings: CustomerRatingSlice{o},
		}
	} else {
		related.R.PartyCustomerRatings = append(related.R.PartyCustomerRatings, o)
	}

	return nil
}

// CustomerRatings retrieves all the records using an executor.
func CustomerRatings(mods ...qm.QueryMod) customerRatingQuery {
	mods = append(mods, qm.From("`CustomerRating`"))
	return customerRatingQuery{NewQuery(mods...)}
}

// FindCustomerRating retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCustomerRating(ctx context.Context, exec boil.ContextExecutor, customerRatingID int, selectCols ...string) (*CustomerRating, error) {
	customerRatingObj := &CustomerRating{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `CustomerRating` where `customer_rating_id`=?", sel,
	)

	q := queries.Raw(query, customerRatingID)

	err := q.Bind(ctx, exec, customerRatingObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from CustomerRating")
	}

	return customerRatingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CustomerRating) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no CustomerRating provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(customerRatingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	customerRatingInsertCacheMut.RLock()
	cache, cached := customerRatingInsertCache[key]
	customerRatingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			customerRatingColumns,
			customerRatingColumnsWithDefault,
			customerRatingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(customerRatingType, customerRatingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(customerRatingType, customerRatingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `CustomerRating` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `CustomerRating` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `CustomerRating` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, customerRatingPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into CustomerRating")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CustomerRatingID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for CustomerRating")
	}

CacheNoHooks:
	if !cached {
		customerRatingInsertCacheMut.Lock()
		customerRatingInsertCache[key] = cache
		customerRatingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CustomerRating.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CustomerRating) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	customerRatingUpdateCacheMut.RLock()
	cache, cached := customerRatingUpdateCache[key]
	customerRatingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			customerRatingColumns,
			customerRatingPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update CustomerRating, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `CustomerRating` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, customerRatingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(customerRatingType, customerRatingMapping, append(wl, customerRatingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update CustomerRating row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for CustomerRating")
	}

	if !cached {
		customerRatingUpdateCacheMut.Lock()
		customerRatingUpdateCache[key] = cache
		customerRatingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q customerRatingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for CustomerRating")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for CustomerRating")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CustomerRatingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), customerRatingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `CustomerRating` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, customerRatingPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in customerRating slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all customerRating")
	}
	return rowsAff, nil
}

var mySQLCustomerRatingUniqueColumns = []string{
	"customer_rating_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CustomerRating) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no CustomerRating provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(customerRatingColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCustomerRatingUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	customerRatingUpsertCacheMut.RLock()
	cache, cached := customerRatingUpsertCache[key]
	customerRatingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			customerRatingColumns,
			customerRatingColumnsWithDefault,
			customerRatingColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			customerRatingColumns,
			customerRatingPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert CustomerRating, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "CustomerRating", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `CustomerRating` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(customerRatingType, customerRatingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(customerRatingType, customerRatingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for CustomerRating")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(customerRatingType, customerRatingMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for CustomerRating")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for CustomerRating")
	}

CacheNoHooks:
	if !cached {
		customerRatingUpsertCacheMut.Lock()
		customerRatingUpsertCache[key] = cache
		customerRatingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CustomerRating record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CustomerRating) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CustomerRating provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), customerRatingPrimaryKeyMapping)
	sql := "DELETE FROM `CustomerRating` WHERE `customer_rating_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from CustomerRating")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for CustomerRating")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q customerRatingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no customerRatingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from CustomerRating")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for CustomerRating")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CustomerRatingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CustomerRating slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(customerRatingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), customerRatingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `CustomerRating` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, customerRatingPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from customerRating slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for CustomerRating")
	}

	if len(customerRatingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CustomerRating) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCustomerRating(ctx, exec, o.CustomerRatingID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CustomerRatingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CustomerRatingSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), customerRatingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `CustomerRating`.* FROM `CustomerRating` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, customerRatingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CustomerRatingSlice")
	}

	*o = slice

	return nil
}

// CustomerRatingExists checks if the CustomerRating row exists.
func CustomerRatingExists(ctx context.Context, exec boil.ContextExecutor, customerRatingID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `CustomerRating` where `customer_rating_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, customerRatingID)
	}

	row := exec.QueryRowContext(ctx, sql, customerRatingID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if CustomerRating exists")
	}

	return exists, nil
}

// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// StatementAmount is an object representing the database table.
type StatementAmount struct {
	StatementAmountID                   int               `boil:"statement_amount_id" json:"statement_amount_id" toml:"statement_amount_id" yaml:"statement_amount_id"`
	StatementID                         int               `boil:"statement_id" json:"statement_id" toml:"statement_id" yaml:"statement_id"`
	StatementAmountCreditDebitIndicator null.String       `boil:"statement_amount_credit_debit_indicator" json:"statement_amount_credit_debit_indicator,omitempty" toml:"statement_amount_credit_debit_indicator" yaml:"statement_amount_credit_debit_indicator,omitempty"`
	StatementAmountType                 null.String       `boil:"statement_amount_type" json:"statement_amount_type,omitempty" toml:"statement_amount_type" yaml:"statement_amount_type,omitempty"`
	Amount                              types.NullDecimal `boil:"amount" json:"amount,omitempty" toml:"amount" yaml:"amount,omitempty"`
	Currency                            null.String       `boil:"currency" json:"currency,omitempty" toml:"currency" yaml:"currency,omitempty"`
	MakerDate                           time.Time         `boil:"maker_date" json:"maker_date" toml:"maker_date" yaml:"maker_date"`
	CheckerDate                         null.Time         `boil:"checker_date" json:"checker_date,omitempty" toml:"checker_date" yaml:"checker_date,omitempty"`
	MakerID                             string            `boil:"maker_id" json:"maker_id" toml:"maker_id" yaml:"maker_id"`
	CheckerID                           null.String       `boil:"checker_id" json:"checker_id,omitempty" toml:"checker_id" yaml:"checker_id,omitempty"`
	ModifiedBy                          string            `boil:"modified_by" json:"modified_by" toml:"modified_by" yaml:"modified_by"`
	ModifiedDate                        time.Time         `boil:"modified_date" json:"modified_date" toml:"modified_date" yaml:"modified_date"`

	R *statementAmountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L statementAmountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var StatementAmountColumns = struct {
	StatementAmountID                   string
	StatementID                         string
	StatementAmountCreditDebitIndicator string
	StatementAmountType                 string
	Amount                              string
	Currency                            string
	MakerDate                           string
	CheckerDate                         string
	MakerID                             string
	CheckerID                           string
	ModifiedBy                          string
	ModifiedDate                        string
}{
	StatementAmountID:                   "statement_amount_id",
	StatementID:                         "statement_id",
	StatementAmountCreditDebitIndicator: "statement_amount_credit_debit_indicator",
	StatementAmountType:                 "statement_amount_type",
	Amount:                              "amount",
	Currency:                            "currency",
	MakerDate:                           "maker_date",
	CheckerDate:                         "checker_date",
	MakerID:                             "maker_id",
	CheckerID:                           "checker_id",
	ModifiedBy:                          "modified_by",
	ModifiedDate:                        "modified_date",
}

// Generated where

var StatementAmountWhere = struct {
	StatementAmountID                   whereHelperint
	StatementID                         whereHelperint
	StatementAmountCreditDebitIndicator whereHelpernull_String
	StatementAmountType                 whereHelpernull_String
	Amount                              whereHelpertypes_NullDecimal
	Currency                            whereHelpernull_String
	MakerDate                           whereHelpertime_Time
	CheckerDate                         whereHelpernull_Time
	MakerID                             whereHelperstring
	CheckerID                           whereHelpernull_String
	ModifiedBy                          whereHelperstring
	ModifiedDate                        whereHelpertime_Time
}{
	StatementAmountID:                   whereHelperint{field: `statement_amount_id`},
	StatementID:                         whereHelperint{field: `statement_id`},
	StatementAmountCreditDebitIndicator: whereHelpernull_String{field: `statement_amount_credit_debit_indicator`},
	StatementAmountType:                 whereHelpernull_String{field: `statement_amount_type`},
	Amount:                              whereHelpertypes_NullDecimal{field: `amount`},
	Currency:                            whereHelpernull_String{field: `currency`},
	MakerDate:                           whereHelpertime_Time{field: `maker_date`},
	CheckerDate:                         whereHelpernull_Time{field: `checker_date`},
	MakerID:                             whereHelperstring{field: `maker_id`},
	CheckerID:                           whereHelpernull_String{field: `checker_id`},
	ModifiedBy:                          whereHelperstring{field: `modified_by`},
	ModifiedDate:                        whereHelpertime_Time{field: `modified_date`},
}

// StatementAmountRels is where relationship names are stored.
var StatementAmountRels = struct {
	Statement string
}{
	Statement: "Statement",
}

// statementAmountR is where relationships are stored.
type statementAmountR struct {
	Statement *Statement
}

// NewStruct creates a new relationship struct
func (*statementAmountR) NewStruct() *statementAmountR {
	return &statementAmountR{}
}

// statementAmountL is where Load methods for each relationship are stored.
type statementAmountL struct{}

var (
	statementAmountColumns               = []string{"statement_amount_id", "statement_id", "statement_amount_credit_debit_indicator", "statement_amount_type", "amount", "currency", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	statementAmountColumnsWithoutDefault = []string{"statement_amount_id", "statement_id", "statement_amount_credit_debit_indicator", "statement_amount_type", "amount", "currency", "maker_date", "checker_date", "maker_id", "checker_id", "modified_by", "modified_date"}
	statementAmountColumnsWithDefault    = []string{}
	statementAmountPrimaryKeyColumns     = []string{"statement_amount_id"}
)

type (
	// StatementAmountSlice is an alias for a slice of pointers to StatementAmount.
	// This should generally be used opposed to []StatementAmount.
	StatementAmountSlice []*StatementAmount
	// StatementAmountHook is the signature for custom StatementAmount hook methods
	StatementAmountHook func(context.Context, boil.ContextExecutor, *StatementAmount) error

	statementAmountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	statementAmountType                 = reflect.TypeOf(&StatementAmount{})
	statementAmountMapping              = queries.MakeStructMapping(statementAmountType)
	statementAmountPrimaryKeyMapping, _ = queries.BindMapping(statementAmountType, statementAmountMapping, statementAmountPrimaryKeyColumns)
	statementAmountInsertCacheMut       sync.RWMutex
	statementAmountInsertCache          = make(map[string]insertCache)
	statementAmountUpdateCacheMut       sync.RWMutex
	statementAmountUpdateCache          = make(map[string]updateCache)
	statementAmountUpsertCacheMut       sync.RWMutex
	statementAmountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var statementAmountBeforeInsertHooks []StatementAmountHook
var statementAmountBeforeUpdateHooks []StatementAmountHook
var statementAmountBeforeDeleteHooks []StatementAmountHook
var statementAmountBeforeUpsertHooks []StatementAmountHook

var statementAmountAfterInsertHooks []StatementAmountHook
var statementAmountAfterSelectHooks []StatementAmountHook
var statementAmountAfterUpdateHooks []StatementAmountHook
var statementAmountAfterDeleteHooks []StatementAmountHook
var statementAmountAfterUpsertHooks []StatementAmountHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *StatementAmount) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *StatementAmount) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *StatementAmount) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *StatementAmount) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *StatementAmount) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *StatementAmount) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *StatementAmount) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *StatementAmount) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *StatementAmount) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range statementAmountAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddStatementAmountHook registers your hook function for all future operations.
func AddStatementAmountHook(hookPoint boil.HookPoint, statementAmountHook StatementAmountHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		statementAmountBeforeInsertHooks = append(statementAmountBeforeInsertHooks, statementAmountHook)
	case boil.BeforeUpdateHook:
		statementAmountBeforeUpdateHooks = append(statementAmountBeforeUpdateHooks, statementAmountHook)
	case boil.BeforeDeleteHook:
		statementAmountBeforeDeleteHooks = append(statementAmountBeforeDeleteHooks, statementAmountHook)
	case boil.BeforeUpsertHook:
		statementAmountBeforeUpsertHooks = append(statementAmountBeforeUpsertHooks, statementAmountHook)
	case boil.AfterInsertHook:
		statementAmountAfterInsertHooks = append(statementAmountAfterInsertHooks, statementAmountHook)
	case boil.AfterSelectHook:
		statementAmountAfterSelectHooks = append(statementAmountAfterSelectHooks, statementAmountHook)
	case boil.AfterUpdateHook:
		statementAmountAfterUpdateHooks = append(statementAmountAfterUpdateHooks, statementAmountHook)
	case boil.AfterDeleteHook:
		statementAmountAfterDeleteHooks = append(statementAmountAfterDeleteHooks, statementAmountHook)
	case boil.AfterUpsertHook:
		statementAmountAfterUpsertHooks = append(statementAmountAfterUpsertHooks, statementAmountHook)
	}
}

// One returns a single statementAmount record from the query.
func (q statementAmountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*StatementAmount, error) {
	o := &StatementAmount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for StatementAmount")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all StatementAmount records from the query.
func (q statementAmountQuery) All(ctx context.Context, exec boil.ContextExecutor) (StatementAmountSlice, error) {
	var o []*StatementAmount

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to StatementAmount slice")
	}

	if len(statementAmountAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all StatementAmount records in the query.
func (q statementAmountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count StatementAmount rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q statementAmountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if StatementAmount exists")
	}

	return count > 0, nil
}

// Statement pointed to by the foreign key.
func (o *StatementAmount) Statement(mods ...qm.QueryMod) statementQuery {
	queryMods := []qm.QueryMod{
		qm.Where("statement_id=?", o.StatementID),
	}

	queryMods = append(queryMods, mods...)

	query := Statements(queryMods...)
	queries.SetFrom(query.Query, "`Statement`")

	return query
}

// LoadStatement allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (statementAmountL) LoadStatement(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStatementAmount interface{}, mods queries.Applicator) error {
	var slice []*StatementAmount
	var object *StatementAmount

	if singular {
		object = maybeStatementAmount.(*StatementAmount)
	} else {
		slice = *maybeStatementAmount.(*[]*StatementAmount)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &statementAmountR{}
		}
		args = append(args, object.StatementID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &statementAmountR{}
			}

			for _, a := range args {
				if a == obj.StatementID {
					continue Outer
				}
			}

			args = append(args, obj.StatementID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Statement`), qm.WhereIn(`statement_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Statement")
	}

	var resultSlice []*Statement
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Statement")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Statement")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Statement")
	}

	if len(statementAmountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Statement = foreign
		if foreign.R == nil {
			foreign.R = &statementR{}
		}
		foreign.R.StatementStatementAmounts = append(foreign.R.StatementStatementAmounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.StatementID == foreign.StatementID {
				local.R.Statement = foreign
				if foreign.R == nil {
					foreign.R = &statementR{}
				}
				foreign.R.StatementStatementAmounts = append(foreign.R.StatementStatementAmounts, local)
				break
			}
		}
	}

	return nil
}

// SetStatement of the statementAmount to the related item.
// Sets o.R.Statement to related.
// Adds o to related.R.StatementStatementAmounts.
func (o *StatementAmount) SetStatement(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Statement) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `StatementAmount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"statement_id"}),
		strmangle.WhereClause("`", "`", 0, statementAmountPrimaryKeyColumns),
	)
	values := []interface{}{related.StatementID, o.StatementAmountID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.StatementID = related.StatementID
	if o.R == nil {
		o.R = &statementAmountR{
			Statement: related,
		}
	} else {
		o.R.Statement = related
	}

	if related.R == nil {
		related.R = &statementR{
			StatementStatementAmounts: StatementAmountSlice{o},
		}
	} else {
		related.R.StatementStatementAmounts = append(related.R.StatementStatementAmounts, o)
	}

	return nil
}

// StatementAmounts retrieves all the records using an executor.
func StatementAmounts(mods ...qm.QueryMod) statementAmountQuery {
	mods = append(mods, qm.From("`StatementAmount`"))
	return statementAmountQuery{NewQuery(mods...)}
}

// FindStatementAmount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindStatementAmount(ctx context.Context, exec boil.ContextExecutor, statementAmountID int, selectCols ...string) (*StatementAmount, error) {
	statementAmountObj := &StatementAmount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `StatementAmount` where `statement_amount_id`=?", sel,
	)

	q := queries.Raw(query, statementAmountID)

	err := q.Bind(ctx, exec, statementAmountObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from StatementAmount")
	}

	return statementAmountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *StatementAmount) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no StatementAmount provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(statementAmountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	statementAmountInsertCacheMut.RLock()
	cache, cached := statementAmountInsertCache[key]
	statementAmountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			statementAmountColumns,
			statementAmountColumnsWithDefault,
			statementAmountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(statementAmountType, statementAmountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(statementAmountType, statementAmountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `StatementAmount` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `StatementAmount` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `StatementAmount` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, statementAmountPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into StatementAmount")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.StatementAmountID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for StatementAmount")
	}

CacheNoHooks:
	if !cached {
		statementAmountInsertCacheMut.Lock()
		statementAmountInsertCache[key] = cache
		statementAmountInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the StatementAmount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *StatementAmount) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	statementAmountUpdateCacheMut.RLock()
	cache, cached := statementAmountUpdateCache[key]
	statementAmountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			statementAmountColumns,
			statementAmountPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update StatementAmount, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `StatementAmount` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, statementAmountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(statementAmountType, statementAmountMapping, append(wl, statementAmountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update StatementAmount row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for StatementAmount")
	}

	if !cached {
		statementAmountUpdateCacheMut.Lock()
		statementAmountUpdateCache[key] = cache
		statementAmountUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q statementAmountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for StatementAmount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for StatementAmount")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o StatementAmountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), statementAmountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `StatementAmount` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, statementAmountPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in statementAmount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all statementAmount")
	}
	return rowsAff, nil
}

var mySQLStatementAmountUniqueColumns = []string{
	"statement_amount_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *StatementAmount) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no StatementAmount provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(statementAmountColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLStatementAmountUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	statementAmountUpsertCacheMut.RLock()
	cache, cached := statementAmountUpsertCache[key]
	statementAmountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			statementAmountColumns,
			statementAmountColumnsWithDefault,
			statementAmountColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			statementAmountColumns,
			statementAmountPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert StatementAmount, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "StatementAmount", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `StatementAmount` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(statementAmountType, statementAmountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(statementAmountType, statementAmountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for StatementAmount")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(statementAmountType, statementAmountMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for StatementAmount")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for StatementAmount")
	}

CacheNoHooks:
	if !cached {
		statementAmountUpsertCacheMut.Lock()
		statementAmountUpsertCache[key] = cache
		statementAmountUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single StatementAmount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *StatementAmount) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no StatementAmount provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), statementAmountPrimaryKeyMapping)
	sql := "DELETE FROM `StatementAmount` WHERE `statement_amount_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from StatementAmount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for StatementAmount")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q statementAmountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no statementAmountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from StatementAmount")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for StatementAmount")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o StatementAmountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no StatementAmount slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(statementAmountBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), statementAmountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `StatementAmount` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, statementAmountPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from statementAmount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for StatementAmount")
	}

	if len(statementAmountAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *StatementAmount) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindStatementAmount(ctx, exec, o.StatementAmountID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *StatementAmountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := StatementAmountSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), statementAmountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `StatementAmount`.* FROM `StatementAmount` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, statementAmountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in StatementAmountSlice")
	}

	*o = slice

	return nil
}

// StatementAmountExists checks if the StatementAmount row exists.
func StatementAmountExists(ctx context.Context, exec boil.ContextExecutor, statementAmountID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `StatementAmount` where `statement_amount_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, statementAmountID)
	}

	row := exec.QueryRowContext(ctx, sql, statementAmountID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if StatementAmount exists")
	}

	return exists, nil
}

// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// AccountType is an object representing the database table.
type AccountType struct {
	AccountTypeID  int         `boil:"account_type_id" json:"account_type_id" toml:"account_type_id" yaml:"account_type_id"`
	BankID         int         `boil:"bank_id" json:"bank_id" toml:"bank_id" yaml:"bank_id"`
	AccountType    string      `boil:"account_type" json:"account_type" toml:"account_type" yaml:"account_type"`
	AccountSubType null.String `boil:"account_sub_type" json:"account_sub_type,omitempty" toml:"account_sub_type" yaml:"account_sub_type,omitempty"`
	Description    null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	Active         null.String `boil:"active" json:"active,omitempty" toml:"active" yaml:"active,omitempty"`

	R *accountTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L accountTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AccountTypeColumns = struct {
	AccountTypeID  string
	BankID         string
	AccountType    string
	AccountSubType string
	Description    string
	Active         string
}{
	AccountTypeID:  "account_type_id",
	BankID:         "bank_id",
	AccountType:    "account_type",
	AccountSubType: "account_sub_type",
	Description:    "description",
	Active:         "active",
}

// Generated where

var AccountTypeWhere = struct {
	AccountTypeID  whereHelperint
	BankID         whereHelperint
	AccountType    whereHelperstring
	AccountSubType whereHelpernull_String
	Description    whereHelpernull_String
	Active         whereHelpernull_String
}{
	AccountTypeID:  whereHelperint{field: `account_type_id`},
	BankID:         whereHelperint{field: `bank_id`},
	AccountType:    whereHelperstring{field: `account_type`},
	AccountSubType: whereHelpernull_String{field: `account_sub_type`},
	Description:    whereHelpernull_String{field: `description`},
	Active:         whereHelpernull_String{field: `active`},
}

// AccountTypeRels is where relationship names are stored.
var AccountTypeRels = struct {
	Bank                string
	AccountTypeAccounts string
}{
	Bank:                "Bank",
	AccountTypeAccounts: "AccountTypeAccounts",
}

// accountTypeR is where relationships are stored.
type accountTypeR struct {
	Bank                *Bank
	AccountTypeAccounts AccountSlice
}

// NewStruct creates a new relationship struct
func (*accountTypeR) NewStruct() *accountTypeR {
	return &accountTypeR{}
}

// accountTypeL is where Load methods for each relationship are stored.
type accountTypeL struct{}

var (
	accountTypeColumns               = []string{"account_type_id", "bank_id", "account_type", "account_sub_type", "description", "active"}
	accountTypeColumnsWithoutDefault = []string{"account_type_id", "bank_id", "account_type", "account_sub_type", "description", "active"}
	accountTypeColumnsWithDefault    = []string{}
	accountTypePrimaryKeyColumns     = []string{"account_type_id"}
)

type (
	// AccountTypeSlice is an alias for a slice of pointers to AccountType.
	// This should generally be used opposed to []AccountType.
	AccountTypeSlice []*AccountType
	// AccountTypeHook is the signature for custom AccountType hook methods
	AccountTypeHook func(context.Context, boil.ContextExecutor, *AccountType) error

	accountTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	accountTypeType                 = reflect.TypeOf(&AccountType{})
	accountTypeMapping              = queries.MakeStructMapping(accountTypeType)
	accountTypePrimaryKeyMapping, _ = queries.BindMapping(accountTypeType, accountTypeMapping, accountTypePrimaryKeyColumns)
	accountTypeInsertCacheMut       sync.RWMutex
	accountTypeInsertCache          = make(map[string]insertCache)
	accountTypeUpdateCacheMut       sync.RWMutex
	accountTypeUpdateCache          = make(map[string]updateCache)
	accountTypeUpsertCacheMut       sync.RWMutex
	accountTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var accountTypeBeforeInsertHooks []AccountTypeHook
var accountTypeBeforeUpdateHooks []AccountTypeHook
var accountTypeBeforeDeleteHooks []AccountTypeHook
var accountTypeBeforeUpsertHooks []AccountTypeHook

var accountTypeAfterInsertHooks []AccountTypeHook
var accountTypeAfterSelectHooks []AccountTypeHook
var accountTypeAfterUpdateHooks []AccountTypeHook
var accountTypeAfterDeleteHooks []AccountTypeHook
var accountTypeAfterUpsertHooks []AccountTypeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AccountType) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AccountType) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AccountType) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AccountType) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AccountType) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AccountType) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AccountType) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AccountType) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AccountType) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountTypeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAccountTypeHook registers your hook function for all future operations.
func AddAccountTypeHook(hookPoint boil.HookPoint, accountTypeHook AccountTypeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		accountTypeBeforeInsertHooks = append(accountTypeBeforeInsertHooks, accountTypeHook)
	case boil.BeforeUpdateHook:
		accountTypeBeforeUpdateHooks = append(accountTypeBeforeUpdateHooks, accountTypeHook)
	case boil.BeforeDeleteHook:
		accountTypeBeforeDeleteHooks = append(accountTypeBeforeDeleteHooks, accountTypeHook)
	case boil.BeforeUpsertHook:
		accountTypeBeforeUpsertHooks = append(accountTypeBeforeUpsertHooks, accountTypeHook)
	case boil.AfterInsertHook:
		accountTypeAfterInsertHooks = append(accountTypeAfterInsertHooks, accountTypeHook)
	case boil.AfterSelectHook:
		accountTypeAfterSelectHooks = append(accountTypeAfterSelectHooks, accountTypeHook)
	case boil.AfterUpdateHook:
		accountTypeAfterUpdateHooks = append(accountTypeAfterUpdateHooks, accountTypeHook)
	case boil.AfterDeleteHook:
		accountTypeAfterDeleteHooks = append(accountTypeAfterDeleteHooks, accountTypeHook)
	case boil.AfterUpsertHook:
		accountTypeAfterUpsertHooks = append(accountTypeAfterUpsertHooks, accountTypeHook)
	}
}

// One returns a single accountType record from the query.
func (q accountTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AccountType, error) {
	o := &AccountType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for AccountType")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AccountType records from the query.
func (q accountTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (AccountTypeSlice, error) {
	var o []*AccountType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AccountType slice")
	}

	if len(accountTypeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AccountType records in the query.
func (q accountTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count AccountType rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q accountTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if AccountType exists")
	}

	return count > 0, nil
}

// Bank pointed to by the foreign key.
func (o *AccountType) Bank(mods ...qm.QueryMod) bankQuery {
	queryMods := []qm.QueryMod{
		qm.Where("bank_id=?", o.BankID),
	}

	queryMods = append(queryMods, mods...)

	query := Banks(queryMods...)
	queries.SetFrom(query.Query, "`Banks`")

	return query
}

// AccountTypeAccounts retrieves all the Account's Accounts with an executor via account_type_id column.
func (o *AccountType) AccountTypeAccounts(mods ...qm.QueryMod) accountQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`Account`.`account_type_id`=?", o.AccountTypeID),
	)

	query := Accounts(queryMods...)
	queries.SetFrom(query.Query, "`Account`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`Account`.*"})
	}

	return query
}

// LoadBank allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (accountTypeL) LoadBank(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccountType interface{}, mods queries.Applicator) error {
	var slice []*AccountType
	var object *AccountType

	if singular {
		object = maybeAccountType.(*AccountType)
	} else {
		slice = *maybeAccountType.(*[]*AccountType)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &accountTypeR{}
		}
		args = append(args, object.BankID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountTypeR{}
			}

			for _, a := range args {
				if a == obj.BankID {
					continue Outer
				}
			}

			args = append(args, obj.BankID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Banks`), qm.WhereIn(`bank_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Bank")
	}

	var resultSlice []*Bank
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Bank")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for Banks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Banks")
	}

	if len(accountTypeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Bank = foreign
		if foreign.R == nil {
			foreign.R = &bankR{}
		}
		foreign.R.BankAccountTypes = append(foreign.R.BankAccountTypes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BankID == foreign.BankID {
				local.R.Bank = foreign
				if foreign.R == nil {
					foreign.R = &bankR{}
				}
				foreign.R.BankAccountTypes = append(foreign.R.BankAccountTypes, local)
				break
			}
		}
	}

	return nil
}

// LoadAccountTypeAccounts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountTypeL) LoadAccountTypeAccounts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccountType interface{}, mods queries.Applicator) error {
	var slice []*AccountType
	var object *AccountType

	if singular {
		object = maybeAccountType.(*AccountType)
	} else {
		slice = *maybeAccountType.(*[]*AccountType)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &accountTypeR{}
		}
		args = append(args, object.AccountTypeID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountTypeR{}
			}

			for _, a := range args {
				if a == obj.AccountTypeID {
					continue Outer
				}
			}

			args = append(args, obj.AccountTypeID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`Account`), qm.WhereIn(`account_type_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on Account")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for Account")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AccountTypeAccounts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &accountR{}
			}
			foreign.R.AccountType = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.AccountTypeID == foreign.AccountTypeID {
				local.R.AccountTypeAccounts = append(local.R.AccountTypeAccounts, foreign)
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.AccountType = local
				break
			}
		}
	}

	return nil
}

// SetBank of the accountType to the related item.
// Sets o.R.Bank to related.
// Adds o to related.R.BankAccountTypes.
func (o *AccountType) SetBank(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Bank) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `AccountType` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"bank_id"}),
		strmangle.WhereClause("`", "`", 0, accountTypePrimaryKeyColumns),
	)
	values := []interface{}{related.BankID, o.AccountTypeID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BankID = related.BankID
	if o.R == nil {
		o.R = &accountTypeR{
			Bank: related,
		}
	} else {
		o.R.Bank = related
	}

	if related.R == nil {
		related.R = &bankR{
			BankAccountTypes: AccountTypeSlice{o},
		}
	} else {
		related.R.BankAccountTypes = append(related.R.BankAccountTypes, o)
	}

	return nil
}

// AddAccountTypeAccounts adds the given related objects to the existing relationships
// of the AccountType, optionally inserting them as new records.
// Appends related to o.R.AccountTypeAccounts.
// Sets related.R.AccountType appropriately.
func (o *AccountType) AddAccountTypeAccounts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Account) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountTypeID = o.AccountTypeID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `Account` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"account_type_id"}),
				strmangle.WhereClause("`", "`", 0, accountPrimaryKeyColumns),
			)
			values := []interface{}{o.AccountTypeID, rel.AccountID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountTypeID = o.AccountTypeID
		}
	}

	if o.R == nil {
		o.R = &accountTypeR{
			AccountTypeAccounts: related,
		}
	} else {
		o.R.AccountTypeAccounts = append(o.R.AccountTypeAccounts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &accountR{
				AccountType: o,
			}
		} else {
			rel.R.AccountType = o
		}
	}
	return nil
}

// AccountTypes retrieves all the records using an executor.
func AccountTypes(mods ...qm.QueryMod) accountTypeQuery {
	mods = append(mods, qm.From("`AccountType`"))
	return accountTypeQuery{NewQuery(mods...)}
}

// FindAccountType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAccountType(ctx context.Context, exec boil.ContextExecutor, accountTypeID int, selectCols ...string) (*AccountType, error) {
	accountTypeObj := &AccountType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `AccountType` where `account_type_id`=?", sel,
	)

	q := queries.Raw(query, accountTypeID)

	err := q.Bind(ctx, exec, accountTypeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from AccountType")
	}

	return accountTypeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AccountType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no AccountType provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	accountTypeInsertCacheMut.RLock()
	cache, cached := accountTypeInsertCache[key]
	accountTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			accountTypeColumns,
			accountTypeColumnsWithDefault,
			accountTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(accountTypeType, accountTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(accountTypeType, accountTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `AccountType` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `AccountType` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `AccountType` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, accountTypePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into AccountType")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.AccountTypeID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for AccountType")
	}

CacheNoHooks:
	if !cached {
		accountTypeInsertCacheMut.Lock()
		accountTypeInsertCache[key] = cache
		accountTypeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AccountType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AccountType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	accountTypeUpdateCacheMut.RLock()
	cache, cached := accountTypeUpdateCache[key]
	accountTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			accountTypeColumns,
			accountTypePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update AccountType, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `AccountType` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, accountTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(accountTypeType, accountTypeMapping, append(wl, accountTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update AccountType row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for AccountType")
	}

	if !cached {
		accountTypeUpdateCacheMut.Lock()
		accountTypeUpdateCache[key] = cache
		accountTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q accountTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for AccountType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for AccountType")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AccountTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `AccountType` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountTypePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in accountType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all accountType")
	}
	return rowsAff, nil
}

var mySQLAccountTypeUniqueColumns = []string{
	"account_type_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AccountType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no AccountType provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountTypeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAccountTypeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	accountTypeUpsertCacheMut.RLock()
	cache, cached := accountTypeUpsertCache[key]
	accountTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			accountTypeColumns,
			accountTypeColumnsWithDefault,
			accountTypeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			accountTypeColumns,
			accountTypePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert AccountType, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "AccountType", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `AccountType` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(accountTypeType, accountTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(accountTypeType, accountTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for AccountType")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(accountTypeType, accountTypeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for AccountType")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for AccountType")
	}

CacheNoHooks:
	if !cached {
		accountTypeUpsertCacheMut.Lock()
		accountTypeUpsertCache[key] = cache
		accountTypeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AccountType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AccountType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AccountType provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), accountTypePrimaryKeyMapping)
	sql := "DELETE FROM `AccountType` WHERE `account_type_id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from AccountType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for AccountType")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q accountTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no accountTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from AccountType")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for AccountType")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AccountTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AccountType slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(accountTypeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `AccountType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountTypePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from accountType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for AccountType")
	}

	if len(accountTypeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AccountType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAccountType(ctx, exec, o.AccountTypeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AccountTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AccountTypeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `AccountType`.* FROM `AccountType` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AccountTypeSlice")
	}

	*o = slice

	return nil
}

// AccountTypeExists checks if the AccountType row exists.
func AccountTypeExists(ctx context.Context, exec boil.ContextExecutor, accountTypeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `AccountType` where `account_type_id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, accountTypeID)
	}

	row := exec.QueryRowContext(ctx, sql, accountTypeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if AccountType exists")
	}

	return exists, nil
}
